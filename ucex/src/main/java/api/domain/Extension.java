package api.domain;

public class Extension {

	private ExtensionCommand command_data;

	public ExtensionCommand getCommand_data() {
		return command_data;
	}

	public void setCommand_data(ExtensionCommand command_data) {
		this.command_data = command_data;
	}
}
